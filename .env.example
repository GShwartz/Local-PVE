# Copy to .env and fill in your values before running docker-compose

# PostgreSQL credentials (used by both the postgres service and backend)
POSTGRES_USER=gil
POSTGRES_PASSWORD=your_password_here

# URL the browser uses to reach the backend (for Vite build-time injection)
VITE_API_BASE=http://your-host:8000
